export const defaultMessage = {
  to: [],
  body: "{shopName} - {title} : {message} until {expiresAt}"
}

export const bdayMessage = {
  to: [],
  body: "{shopName} - happy birthday {customerName} : {message} until {expiresAt}"
}